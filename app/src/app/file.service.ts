import { Injectable } from '@angular/core';
import { LayoutService } from './layout.service';

@Injectable({
  providedIn: 'root',
})
export class FileService {
  constructor() {}

  registerActions(layout: LayoutService) {}
}
